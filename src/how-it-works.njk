---
layout: base.njk
pageTitle: "How it works"
pageDescription: "How to join, post items, browse, and collect through Sportswear X-Change."
navActive: how
navBg: peach
permalink: /how-it-works/
---

<!-- Page hero (peach) -->
<div class="page-hero section-peach">
  <div class="page-hero-inner">
    <div class="page-hero-display">How it<br>works.</div>
    <p class="page-hero-sub">{{ how_it_works.hero_intro }}</p>
  </div>
</div>

<!-- Steps (orange) -->
<div class="steps-section">
  <div class="steps-headline-centered">
    <img src="/assets/ellipse-stroke.svg" class="section-ellipse" alt="" aria-hidden="true">
    <span>3 simple steps</span>
    <img src="/assets/ellipse-stroke-2.svg" class="section-ellipse" alt="" aria-hidden="true">
  </div>
  <div class="steps-list">
    {% for step in how_it_works.steps %}
    <div class="step-item">
      <div class="step-rule"></div>
      <div class="step-row">
        <span class="step-num">0{{ step.num }}.</span>
        <div class="step-body">
          <div class="step-title">{{ step.title }}</div>
          <p class="step-desc">{{ step.description }}</p>
        </div>
      </div>
    </div>
    {% endfor %}
    <div class="step-rule"></div>
  </div>

  <div class="steps-cta">
    <div class="steps-cta-text">Scan the code to join our WhatsApp!</div>
    <div class="steps-cta-right">
      <img src="/assets/qrcode.png" alt="Scan to join WhatsApp group" class="qr-code">
      <a class="nav-join-outline" href="{{ site.whatsapp_url }}" target="_blank" rel="noopener">Join our WhatsApp Group</a>
    </div>
  </div>
</div>

<!-- Drop-off locations (peach) -->
<div class="locations-section section-peach">
  <div class="locations-rule"></div>
  <div class="locations-inner">
    <div class="locations-heading">Dropoff locations</div>
    <div class="locations-list">
      {% for point in how_it_works.collection_points %}
      <div class="location-name">{{ point }}</div>
      <div class="location-rule"></div>
      {% endfor %}
      <p class="t-body">{{ how_it_works.collection_points_note }} <a href="/get-involved/#businesses" style="text-decoration:underline;">Get in touch</a></p>
    </div>
  </div>
</div>

<!-- Get involved banner (peach) -->
<div class="involved-banner section-peach">
  <div class="involved-card">
    <div class="involved-card-text">Want to get involved and become a dropoff location?</div>
    <a class="btn-outline-white" href="/get-involved/#businesses">Find out more →</a>
  </div>
</div>

<!-- What can / can't / community rules (orange) -->
<div class="section-orange section-inner">
  <div class="step-rule" style="background:var(--plum)"></div>
  <div class="step-row" style="align-items:flex-start">
    <div style="flex:0 0 300px">
      <div class="t-display" style="color:var(--plum)">What can be swapped</div>
    </div>
    <ul style="flex:1;list-style:none;display:flex;flex-direction:column;gap:8px;">
      {% for item in how_it_works.what_can %}
      <li class="t-body" style="color:var(--plum)">— {{ item }}</li>
      {% endfor %}
    </ul>
  </div>
  <div class="step-rule" style="background:var(--plum)"></div>
  <div class="step-row" style="align-items:flex-start">
    <div style="flex:0 0 300px">
      <div class="t-display" style="color:var(--plum)">What can't be swapped</div>
    </div>
    <ul style="flex:1;list-style:none;display:flex;flex-direction:column;gap:8px;">
      {% for item in how_it_works.what_cannot %}
      <li class="t-body" style="color:var(--plum)">— {{ item }}</li>
      {% endfor %}
    </ul>
  </div>
  <div class="step-rule" style="background:var(--plum)"></div>
</div>

<!-- FAQs (blue) -->
<div class="section-blue section-inner">
  <div class="steps-headline-centered">
    <img src="/assets/ellipse-stroke.svg" class="section-ellipse" alt="" aria-hidden="true">
    <span>FAQs</span>
    <img src="/assets/ellipse-stroke-2.svg" class="section-ellipse" alt="" aria-hidden="true">
  </div>
  <div style="display:flex;flex-direction:column;gap:0;">
    {% for faq in how_it_works.faqs %}
    <div class="faq-item">
      <div class="faq-q">{{ faq.q }}</div>
      <p class="faq-a">{{ faq.a }}</p>
    </div>
    {% endfor %}
  </div>
</div>
